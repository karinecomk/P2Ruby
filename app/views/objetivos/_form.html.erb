<%= simple_form_for(@objetivo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :dias %>
    <%= f.input :peso %>
  </div>

  <div class="form-actions">
    <%= f.submit 'Salvar' %>
  </div>
<% end %>
